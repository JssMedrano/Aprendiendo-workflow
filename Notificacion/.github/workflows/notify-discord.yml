name: Notificación en Discord

on: 
  push:          # Se activa al hacer push
  workflow_run:  # Opcional: Notifica cuando un workflow termina
    workflows: ["Tests"]  # Reemplaza "Tests" con el nombre de tu workflow de pruebas
    types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar mensaje a Discord
        run: |
          curl -X POST \
          -H "Content-Type: application/json" \
          -d '{"content":"✅ ¡El código pasó las pruebas! 🎉 Repo: ${{ github.repository }} | Commit: ${{ github.sha }}"}' \
          ${{ secrets.DISCORD_WEBHOOK_URL }}
